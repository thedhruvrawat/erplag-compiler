<<module arraysum>>
takes input[list:array[4..20] of real];
returns [sum:real];
start
    declare s: real;
    s := 5.4321e-3;
    declare index : integer;
    for (index in 4..20)
    start
        s := s + list[index];
    end
    sum := s;
end
<<<driver program>>>
start
    declare num, s:real;
    declare A:array [4..20] of real;
    for( k in 6..10)
    start
        get_value(num);       ** ERROR : get_value cannot read any array element(implicitly, a complete array) or any static constant(denied by grammar) **
        **get_value(A[k]);   ERROR : Same as above, but this case gives Parsing Error, which IG is fine, given our code generates some error to not allow it **
    end
    print(num);
end