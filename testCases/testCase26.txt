**declare module arraysum;  Arraysum is defined before array1sum **
<<<driver program>>>
start
	declare A, B:array [-10..10] of integer;
	declare a, b : integer;
	a := 5;
	A[5 * a + 6 * b] := a * b + 6 * a + B[a * b + 7];
	print(A[a]);

	declare c: boolean;

	switch(c)
	start
		case true:
			a := b;
			break;
		case false:
			b := a * b + 5;
			break;
	end

	while (a > 5)
	start
		declare k: integer;

		a := a + 1;
		b := b + 1;
	end
end